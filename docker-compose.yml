version: '3'
services:
  node:
    build: .
    restart: always
    ports:
            - "10000:10000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/home/node
      - /home/node/node_modules
